import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AppShell } from "@/components/AppShell";
import { PAPER_LABEL, DISCLAIMER } from "@/lib/constants";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Paper Coach — simulated trading classroom",
  description: DISCLAIMER,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="paper-grid min-h-full bg-background text-foreground">
        <a className="sr-only" href="#main">
          Skip to content
        </a>
        <AppShell paperLabel={PAPER_LABEL}>{children}</AppShell>
      </body>
    </html>
  );
}
