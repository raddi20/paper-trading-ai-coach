# Optional. If set, the coach may rewrite explanations in friendlier prose.
# The app never requires this — templated explanations always work.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# No broker keys. No live trading. Do not add brokerage credentials here.
