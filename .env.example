# Optional. If set, Coach Chat and dashboard notes may use friendlier LLM wording.
# The app never requires this — templated / rule-based coaching always works.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# No broker keys. No live trading. Do not add brokerage credentials here.
